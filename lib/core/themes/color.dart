import 'package:flutter/material.dart';

// TODO: rename color names
class ColorsManager {
  static const primary = Color(0xFF636AE8);
  static const border = Color(0xFF565D6D);
  static const outgoingBox = Color(0xFF2A2D44);
  static const incomingBox = Color(0xFF20242D);
  static const dimmedText = Color(0xFF9095A1);
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);

  ColorsManager._();
}
